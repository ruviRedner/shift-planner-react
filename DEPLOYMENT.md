# פריסה ב־Render עם שמירה קבועה

הקובץ `render.yaml` מגדיר אתר Node.js שמגיש גם את האתר וגם את ה־API, עם דיסק קבוע לשמירת החשבונות והסידור. השירות והדיסק דורשים מסלול בתשלום. אין צורך להשאיר את המחשב המקומי דולק.

1. העלו את הפרויקט למאגר GitHub שלכם, בלי תיקיית `.planner-data` ובלי סודות.
2. ב־Render בחרו New → Blueprint וחברו את המאגר.
3. בדקו את המחיר המוצג ואשרו יצירת השירות. ההגדרות נטענות מ־`render.yaml`.
4. בסיום הפריסה פתחו את כתובת האתר שמופיעה ב־Render.
5. צרו חשבון מנהל עם קוד ההתקנה שבמשתנה `PLANNER_SETUP_TOKEN` בהגדרות Environment של השירות. שמרו את הקוד פרטי.

בפריסה זו האתר נפתח ישירות בכניסה לחשבון. לאחר ההתחברות השינויים נשמרים בשרת בקובץ `/var/data/team.json`, על הדיסק הקבוע. סגירת הדפדפן אינה מוחקת אותם. אחרי הפעלה מחדש של השרת ייתכן שתצטרכו להתחבר שוב; החשבונות והסידור נשמרים.

אין לשנות את נתיב הנתונים לתיקייה מחוץ לדיסק או להסיר את הדיסק. שמירה קבועה אינה תחליף לגיבוי: הורידו גיבויי סידור דרך האפליקציה, ובדקו את אפשרויות גיבוי ושחזור הדיסק ב־Render. מחיקת השירות או הדיסק עלולה למחוק את הנתונים.

## העברת מידע קיים

נתוני localhost אינם עוברים אוטומטית לאתר החדש. בדפדפן המקורי פתחו את הסידור הישן וייצאו גיבוי. באתר החדש התחברו כמנהל וייבאו את הגיבוי לסידור המשותף. בדקו את הסידור לפני המשך עבודה. כפתור העברה מהדפדפן לשרת מתאים רק לנתונים השמורים באותה כתובת אתר.

## הפעלה עם npm

נדרש Node.js 24 ומעלה.

```bash
npm install
npm run dev:full
```

לבניית האתר ולהרצת שרת שמגיש את הבנייה:

```bash
npm run build
npm start
```

לבדיקת התנהגות הפריסה המקומית הגדירו `VITE_SHARED_ONLY=true` בסביבת הבנייה. בלי המשתנה, מצב הדפדפן המקומי נשאר זמין לפיתוח ולהוצאת גיבויים ישנים. עצירה באמצעות Ctrl+C.

Netlify כאתר סטטי בלבד אינו מפעיל את שרת Node של הפרויקט. ההגדרה המצורפת מיועדת ל־Render.

תיעוד: https://render.com/docs/disks ו־https://render.com/docs/blueprint-spec
