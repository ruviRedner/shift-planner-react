# פריסה חינמית: Render ו־Neon

render.yaml מגדיר שירות Node.js במסלול Free שמגיש את האתר ואת ה־API. הנתונים נשמרים ב־PostgreSQL חיצוני, למשל במסלול Free של Neon.

## הקמה

1. צרו פרויקט Free ב־Neon והעתיקו כתובת חיבור ל־PostgreSQL עם SSL.
2. ב־Render בחרו New → Blueprint וחברו את מאגר הפרויקט בענף main.
3. הזינו את כתובת החיבור במשתנה הסודי DATABASE_URL. אין להעלות אותה ל־GitHub או למשתני VITE_.
4. ודאו שהשירות במסלול Free וללא דיסק בתשלום ואשרו פריסה.
5. פתחו את האתר לאחר שהשירות מגיע למצב Live.
6. האתר נפתח ישירות לסידור, ללא חשבון או קוד התקנה. כל מי שיש לו את הקישור יכול לצפות ולערוך. הנתונים נשמרים בשרת.

השרת יוצר אוטומטית טבלת shift_planner_state. החשבונות, ההזמנות, הצוות והסידור נשמרים בה. השמירה מאושרת רק לאחר אישור מסד הנתונים. עדכונים מקבילים משתמשים בנעילת רשומה בתוך טרנזקציה כדי למנוע דריסה.

ב־Render השרת מסרב לעלות ללא DATABASE_URL כדי למנוע שמירה בטעות בקובץ זמני. סגירת דפדפן או הפעלה מחדש של האתר אינה מוחקת את מסד הנתונים. אחרי הפעלה מחדש ייתכן שתצטרכו להתחבר שוב.

## מגבלות וגיבוי

Render Free נרדם בחוסר פעילות, ולכן הכניסה הבאה יכולה להיות איטית. חלים גבולות שימוש של Render ושל Neon. מסד הנתונים החינמי של Render פג אחרי 30 יום ולכן אינו מתאים לשמירה ארוכת טווח כאן.

הורידו גיבויי סידור מהאפליקציה מעת לעת. מסלול חינמי אינו מבטיח שמירה לנצח; מחיקת פרויקט מסד הנתונים מוחקת גם את המידע שבו.

## העברת מידע קיים

נתוני localhost אינם עוברים אוטומטית לאתר החדש. בדפדפן המקורי ייצאו גיבוי מהסידור הישן. באתר החדש ייבאו את הגיבוי לסידור המשותף. בדקו שהנתונים עברו לפני המשך עבודה.

## הפעלה מקומית

נדרש Node.js 24 ומעלה. הריצו npm install ואחריו npm run dev:full. ללא DATABASE_URL הנתונים המקומיים של השרת נשמרים ב־.planner-data/team.json. עצירה באמצעות Ctrl+C.

לבנייה: npm run build. להרצת הבנייה: npm start.

VITE_SHARED_ONLY=true בזמן הבנייה פותח ישירות את האזור המשותף; כבר מוגדר בפריסה.

תיעוד: https://render.com/docs/free ו־https://neon.com/docs/introduction/plans

PUBLIC_EDITING=true בשרת ו־VITE_PUBLIC_EDITING=true בבנייה מאפשרים צפייה ועריכה ללא התחברות. שניהם מוגדרים בפריסה. במצב זה חשבונות והזמנות אינם נחשפים לציבור.
